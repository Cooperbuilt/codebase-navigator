# Repository Inventory

> This file is auto-generated by `/setup` and can be manually edited.
> It tells the plugin which repos to search and what each one contains.

## api-gateway

- **Purpose:** Public-facing REST API. Next.js BFF that handles authentication, request validation, and routes to backend services.
- **Language/Framework:** TypeScript / Next.js
- **Type:** api
- **Key entry points:** `src/app/api/` route handlers, `src/middleware.ts`
- **Notes:** All external traffic enters through here. Uses NextAuth for session management.

## worker-service

- **Purpose:** Async background processing. Consumes SQS messages for order processing, email sends, and data aggregation.
- **Language/Framework:** Python 3.12 / Lambda
- **Type:** worker
- **Key entry points:** `src/handlers/` — one handler per queue consumer
- **Notes:** Each handler is a separate Lambda function. Shared utilities in `src/lib/`.

## infra

- **Purpose:** Terraform infrastructure definitions for all AWS resources.
- **Language/Framework:** Terraform / HCL
- **Type:** infrastructure
- **Key entry points:** `modules/` — one module per service, `environments/` — per-env variable files
- **Notes:** State stored in S3. Uses Terraform workspaces for dev/staging/prod.

## shared-lib

- **Purpose:** Shared Python utilities used across backend services. Auth helpers, data models, common validators.
- **Language/Framework:** Python 3.12
- **Type:** library
- **Key entry points:** `src/` — importable modules
- **Notes:** Published as internal package. Version pinned in consumer services.
